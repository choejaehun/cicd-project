apiVersion: v1
kind: Service
metadata:
  name: nginx-rollout-service
spec:
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: nginx-rollout
  externalTrafficPolicy: Cluster
  sessionAffinity: None
  type: LoadBalancer
